cd 파일명(디렉토리)-경로지정
git init -깃 초기화 (로컬파일지정)

git config -- global user.name "깃허브아이디"
git config -- global user.email 깃허브이메일

git remote add origin "깃 리포지토리 링크"
git add 파일명 
git commit -m "커밋메모"
git add push origin [master,main]